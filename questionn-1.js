// 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

let a = "Hiren";
let b = true;
let c;
let d = null;


console.log(
    `The variable is ${a} and its type is ${typeof(a)}
    The variable is ${b} and its type is ${typeof(b)}
    The variable is ${c} and its type is ${typeof(c)}
    The variable is ${d} and its type is ${typeof(d)}`
)